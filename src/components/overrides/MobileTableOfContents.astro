---
/**
 * MobileTableOfContents override — intentionally empty.
 * Hides the mobile "On this page" outline panel from all docs pages.
 * A side-wide navigation is used instead (see CustomPageFrame.astro override).
 */
---
