{
  "name": "snippet-emitter",
  "private": true,
  "type": "module",
  "description": "The product's code generating renderer, bundled from the published packages so the checks need nothing but npm.",
  "scripts": {
    "build": "esbuild snippet-api.ts --bundle --platform=node --format=cjs --outfile=dist/snippet-api.cjs --log-level=warning"
  },
  "dependencies": {
    "igniteui-webcomponents-core": "7.1.1-beta.11"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
