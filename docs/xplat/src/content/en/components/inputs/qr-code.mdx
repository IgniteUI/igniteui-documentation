---
title: "QR Code"
description: "The {ProductName} QR Code renders a scannable QR (Quick Response) code as an inline SVG, generated on the client from any string value, with customizable size, error correction, and center logo."
keywords: "{Platform} QR Code, {ProductName}, QR code, scannable code, SVG QR code, QR generator, logo, error correction"
license: MIT
mentionedTypes: ["QrCode"]
relatedComponents: []
last_updated: "2026-09-08"
llms:
  description: "The {ProductName} QR Code renders a scannable QR (Quick Response) code as an inline SVG, generated on the client from any string value, with customizable size, error correction, and center logo."
---
import PlatformBlock from 'igniteui-astro-components/components/mdx/PlatformBlock.astro';
import Sample from 'igniteui-astro-components/components/mdx/Sample.astro';
import ApiLink from 'igniteui-astro-components/components/mdx/ApiLink.astro';
import Anatomy from 'igniteui-astro-components/components/mdx/Anatomy.astro';
import Badge from 'igniteui-astro-components/components/mdx/Badge.astro';
import Faq from 'igniteui-astro-components/components/mdx/Faq.astro';
import FaqItem from 'igniteui-astro-components/components/mdx/FaqItem.astro';
{/* TODO: uncomment when images are available
import { Image } from 'astro:assets';
import qrCodeAnatomy from '@xplat-images/anatomy-content-light/qr-code-lt-a.png';
import qrCodeDo from '@xplat-images/qr-code/qr-code-do.png';
import qrCodeDo2x from '@xplat-images/qr-code/qr-code-do@2x.png';
import qrCodeDoNot from '@xplat-images/qr-code/qr-code-do-not.png';
import qrCodeDoNot2x from '@xplat-images/qr-code/qr-code-do-not@2x.png';
*/}

# QR Code Component <Badge variant="new" />

The {ProductName} QR Code renders a scannable QR (Quick Response) code as an inline SVG, generated on the client from any string value, with customizable size, error correction, and center logo.

## Live Demo

The {Platform} QR Code demo shows a basic scannable code generated from a URL string.

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-overview" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/overview" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code example" />

</PlatformBlock>

## Anatomy

The {Platform} QR Code is a square SVG graphic containing three finder-pattern corners, the encoded data modules, an optional quiet-zone margin, and an optional center logo image.

{/* TODO: replace with verified anatomy image
<Anatomy
  class="qr-code-anatomy"
  name="QR Code"
  description="The QR Code component is a square SVG graphic containing finder-pattern corners, data modules, a quiet-zone margin, and an optional center logo."
  src={qrCodeAnatomy.src}
  alt="QR Code anatomy"
/>

<style>{`
  .qr-code-anatomy {
    --igd-anatomy-padding: 0px;
  }
`}</style>
*/}

<span class="ig-typography__body-2" style="display: block; margin-bottom: 24px;"><strong>1. Finder-pattern corners:</strong> Three square markers in the top-left, top-right, and bottom-left, used by scanners to orient the code.<br />
<strong>2. Data modules:</strong> The dots that encode the string value.<br />
<strong>3. Quiet-zone margin:</strong> The blank area around the code required for reliable scanning.<br />
<strong>4. Center logo:</strong> An optional image overlaid at the center of the code, with the modules beneath it automatically masked.</span>

<PlatformBlock for="WebComponents, React, Blazor">

```text
<igc-qr-code role="img">
└── svg
    ├── <title>                          // "QR code: <value>" or explicit aria-label
    ├── ::part(background)               // background rect
    ├── ::part(dots)                     // data modules path
    ├── ::part(corner-square)            // outer finder-pattern squares (×3)
    ├── ::part(corner-dot)               // inner finder-pattern dots (×3)
    ├── <mask>                           // rendered when `logo-src` is set
    └── <image>                          // center logo, rendered when `logo-src` is set
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```text
<igx-qr-code role="img">
└── svg
    ├── <title>                          // "QR code: <value>" or explicit ariaLabel
    ├── background                       // background rect
    ├── dots                             // data modules path
    ├── corner-square                    // outer finder-pattern squares (×3)
    ├── corner-dot                       // inner finder-pattern dots (×3)
    ├── mask                             // rendered when `logoSrc` is set
    └── image                            // center logo, rendered when `logoSrc` is set
</igx-qr-code>
```

</PlatformBlock>

## Getting Started

To use the {Platform} QR Code, follow the [{ProductName} Getting Started](../general-getting-started.mdx) topic for the basic project setup, then register the component for your target platform.

### Prerequisites and Version Compatibility

The QR Code component is available in **igniteui-webcomponents 7.3.0** and later, and the corresponding wrapper releases for React, Blazor, and Angular. Keep the framework package, the QR Code package, and the theme package on the same release version.

<PlatformBlock for="WebComponents">

For Web Components using the **{PackageWebComponents}** package, install the package:

```cmd
npm install {PackageWebComponents}
```

Import the <ApiLink pkg="core" type="QrCode" />, its theme CSS, and register the component:

```ts
import { defineComponents, IgcQrCodeComponent } from 'igniteui-webcomponents';
import 'igniteui-webcomponents/themes/light/bootstrap.css';

defineComponents(IgcQrCodeComponent);
```

</PlatformBlock>

<PlatformBlock for="React">

For React using the **igniteui-react** package, install the package:

```cmd
npm install igniteui-react
```

Import the QR Code wrapper and theme CSS:

```tsx
import { IgrQrCode } from 'igniteui-react';
import 'igniteui-webcomponents/themes/light/bootstrap.css';
```

</PlatformBlock>

<PlatformBlock for="Blazor">

For Blazor using the **IgniteUI.Blazor** package, register the QR Code module:

```csharp
// in Program.cs file

builder.Services.AddIgniteUIBlazor(typeof(IgbQrCodeModule));
```

Link the additional CSS file that applies the styling to the <ApiLink pkg="core" type="QrCode" /> component in **wwwroot/index.html** (Blazor Web Assembly) or **Pages/_Host.cshtml** (Blazor Server):

```razor
<link href="_content/IgniteUI.Blazor/themes/light/bootstrap.css" rel="stylesheet" />
```

</PlatformBlock>

<PlatformBlock for="Angular">

For Angular using the **igniteui-angular** package, install the package:

```cmd
npm install igniteui-angular
```

Then import the QR Code component:

```ts
import { IgxQrCodeComponent } from 'igniteui-angular/qr-code';
```

Add `IgxQrCodeComponent` to the component `imports` collection, then use the `igx-qr-code` element in your template.

</PlatformBlock>

<PlatformBlock for="WebComponents">

The simplest way to start using the <ApiLink pkg="core" type="QrCode" /> is as follows:

```html
<igc-qr-code value="https://www.infragistics.com"></igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

The simplest way to start using the <ApiLink pkg="core" type="QrCode" /> is as follows:

```tsx
<IgrQrCode value="https://www.infragistics.com"></IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

The simplest way to start using the <ApiLink pkg="core" type="QrCode" /> is as follows:

```razor
<IgbQrCode Value="https://www.infragistics.com"></IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

The simplest way to start using the <ApiLink type="QrCode" /> is as follows:

```html
<igx-qr-code value="https://www.infragistics.com"></igx-qr-code>
```

</PlatformBlock>

By default, this renders a 128×128 pixel QR code with a 4-module quiet-zone margin, `M` error correction, and square modules and corners.

## Usage

Set the <ApiLink pkg="core" type="QrCode" member="value" label="value" /> property to any string to render a scannable QR code. The component selects the most compact encoding mode (numeric, alphanumeric, or byte) and the smallest QR version that fits the value automatically. Clearing `value` removes the SVG entirely.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code value="Hello, World!"></igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode value="Hello, World!"></IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode Value="Hello, World!"></IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code value="Hello, World!"></igx-qr-code>
```

</PlatformBlock>

### Version and Error Correction

Use the <ApiLink pkg="core" type="QrCode" member="errorLevel" label="errorLevel" /> property to choose between `L`, `M`, `Q`, and `H` error correction levels. Higher levels are more resilient to damage or obstruction but produce a larger, denser code. Use the <ApiLink pkg="core" type="QrCode" member="version" label="version" /> property to pin a specific QR version between `1` and `40` when you need predictable code dimensions; leave it unset to let the component pick the smallest version that fits the value.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  version="4"
  error-level="H">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com"
  version={4}
  errorLevel="H">
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  Version="4"
  ErrorLevel="QrCodeErrorLevel.H">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  [version]="4"
  errorLevel="H">
</igx-qr-code>
```

</PlatformBlock>

### Size and Margin

Use the <ApiLink pkg="core" type="QrCode" member="size" label="size" /> property to set the rendered pixel width and height and the <ApiLink pkg="core" type="QrCode" member="margin" label="margin" /> property to control the quiet-zone margin in QR modules around the code. Neither property changes the encoded data.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  size="256"
  margin="2">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com"
  size={256}
  margin={2}>
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  Size="256"
  Margin="2">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  [size]="256"
  [margin]="2">
</igx-qr-code>
```

</PlatformBlock>

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-sizing" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code sizing example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/sizing" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code sizing example" />

</PlatformBlock>

### Center Logo

Add a branded center image with the <ApiLink pkg="core" type="QrCode" member="logoSrc" label="logoSrc" /> property. The component automatically masks the modules beneath the image so the code stays scannable. Use <ApiLink pkg="core" type="QrCode" member="logoSize" label="logoSize" /> — a ratio between `0` and `1` — to control how much of the safe area the logo covers, and <ApiLink pkg="core" type="QrCode" member="logoMargin" label="logoMargin" /> to add whitespace around the logo in pixels.

When `errorLevel` is not explicitly set, the component automatically escalates to the smallest error correction level that keeps the code scannable at the requested `logoSize`.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  logo-src="/assets/logo.png"
  logo-size="0.5"
  logo-margin="4">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com"
  logoSrc="/assets/logo.png"
  logoSize={0.5}
  logoMargin={4}>
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  LogoSrc="/assets/logo.png"
  LogoSize="0.5"
  LogoMargin="4">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  logoSrc="/assets/logo.png"
  [logoSize]="0.5"
  [logoMargin]="4">
</igx-qr-code>
```

</PlatformBlock>

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-logo" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code logo example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/logo" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code logo example" />

</PlatformBlock>

For safety, `logoSrc` values that use unsafe schemes (such as `javascript:` or `vbscript:`) or non-image `data:` URIs are rejected — no image is rendered. Regular URLs and `data:image/*` URIs are accepted.

### Dot and Corner Shapes

Use <ApiLink pkg="core" type="QrCode" member="dotStyle" label="dotStyle" /> to shape the data modules and the inner finder-pattern dots, and <ApiLink pkg="core" type="QrCode" member="squareStyle" label="squareStyle" /> to shape the outer finder-pattern squares. Each accepts `square` (default), `circle`, or `rounded`.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  dot-style="rounded"
  square-style="circle">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com"
  dotStyle="rounded"
  squareStyle="circle">
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  DotStyle="QrCodeDotStyle.Rounded"
  SquareStyle="QrCodeSquareStyle.Circle">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  dotStyle="rounded"
  squareStyle="circle">
</igx-qr-code>
```

</PlatformBlock>

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-shapes" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code shapes example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/shapes" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code shapes example" />

</PlatformBlock>

### Custom Colors

Style the background and module colors through the exposed CSS custom properties. The QR Code colors do not automatically invert when the global theme switches to a dark variant — set them explicitly if you want a dark-themed code.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  style="
    --ig-qr-code-background: #e8f4ff;
    --ig-qr-code-dark-color: #0066cc;
    --ig-qr-code-corner-square-color: #003366;
  ">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com"
  style={{
    ['--ig-qr-code-background' as any]: '#e8f4ff',
    ['--ig-qr-code-dark-color' as any]: '#0066cc',
    ['--ig-qr-code-corner-square-color' as any]: '#003366',
  }}>
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  Style="--ig-qr-code-background: #e8f4ff; --ig-qr-code-dark-color: #0066cc; --ig-qr-code-corner-square-color: #003366;">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  style="
    --ig-qr-code-background: #e8f4ff;
    --ig-qr-code-dark-color: #0066cc;
    --ig-qr-code-corner-square-color: #003366;
  ">
</igx-qr-code>
```

</PlatformBlock>

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-colors" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code custom colors example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/colors" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code custom colors example" />

</PlatformBlock>

### Accessible Labeling

The rendered `<svg>` has `role="img"` and includes a `<title>` element that is announced by screen readers. The default title is `QR code: <value>`. Provide a custom `aria-label` to override the announced text with a more descriptive phrase.

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com/products"
  aria-label="Scan to visit our product page">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```tsx
<IgrQrCode
  value="https://www.infragistics.com/products"
  ariaLabel="Scan to visit our product page">
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com/products"
  AriaLabel="Scan to visit our product page">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com/products"
  ariaLabel="Scan to visit our product page">
</igx-qr-code>
```

</PlatformBlock>

### Do/Don't

**When to use:** Use the QR Code to encode a URL, identifier, payment reference, one-time setup token, vCard, or any string that a mobile scanner can act on. Add a center logo for branded codes on marketing collateral, packaging, or ticketing.

**When not to use:** Do not use the QR Code as a substitute for editable text, a form input, or an interactive control — the component is a static, presentational graphic. Do not encode secrets or sensitive data that must not be captured by a camera.

{/* TODO: replace with guidance images from Indigo.Design once available
<div class="table-responsive">
  <table class="table" style="width: 100%; table-layout: fixed; border-collapse: collapse; margin-bottom: 24px;">
    <tbody>
      <tr>
        <td style="border: none; padding: 16px 20px;"><Image src={qrCodeDo} srcset={`${qrCodeDo.src} 1x, ${qrCodeDo2x.src} 2x`} alt="QR Code used with a labelled call to action" class="responsive-img" style="border: 1px solid #D2DBE8; margin: unset;" /></td>
        <td style="border: none; padding: 16px 20px;"><Image src={qrCodeDoNot} srcset={`${qrCodeDoNot.src} 1x, ${qrCodeDoNot2x.src} 2x`} alt="QR Code used without context or accessible label" class="responsive-img" style="border: 1px solid #D2DBE8; margin: unset;" /></td>
      </tr>
      <tr>
        <td style="border: none; padding: 16px 20px; text-align: left; color: green;"><strong>Do</strong></td>
        <td style="border: none; padding: 16px 20px; text-align: left; color: red;"><strong>Don't</strong></td>
      </tr>
      <tr>
        <td style="border: none; padding: 4px 20px; text-align: left; text-wrap: auto;">Pair the QR Code with a short call to action and a descriptive `ariaLabel` so users know what they will get after scanning.</td>
        <td style="border: none; padding: 4px 20px; text-align: left; text-wrap: auto;">Avoid oversized logos or low error correction on codes that will be printed, photographed, or displayed on damaged surfaces.</td>
      </tr>
    </tbody>
  </table>
</div>
*/}

## Properties

The {Platform} QR Code exposes properties for the encoded value, code density, sizing, logo, and shape customization.

<PlatformBlock for="WebComponents, React, Blazor">

| Name | Type | Default | Description |
| -- | -- | -- | -- |
| <ApiLink pkg="core" type="QrCode" member="value" label="value" /> | `string` | — | The string encoded in the QR code. Clearing the value removes the SVG. |
| <ApiLink pkg="core" type="QrCode" member="version" label="version" /> | `number` | — | The QR version (1–40) used to generate the code. When not set, the smallest version that fits `value` is chosen automatically. |
| <ApiLink pkg="core" type="QrCode" member="errorLevel" label="errorLevel" /> | `'L' \| 'M' \| 'Q' \| 'H'` | `'M'` | The error correction level used when generating the QR code. |
| <ApiLink pkg="core" type="QrCode" member="size" label="size" /> | `number` | `128` | The rendered width and height of the QR code in pixels. |
| <ApiLink pkg="core" type="QrCode" member="margin" label="margin" /> | `number` | `4` | The quiet-zone margin around the code, expressed in QR modules. |
| <ApiLink pkg="core" type="QrCode" member="logoSrc" label="logoSrc" /> | `string` | — | The source URL of an optional logo image rendered at the center of the QR code. |
| <ApiLink pkg="core" type="QrCode" member="logoSize" label="logoSize" /> | `number` | `0.4` | The logo size as a ratio (0–1) of the safe area that can be obscured while remaining scannable. |
| <ApiLink pkg="core" type="QrCode" member="logoMargin" label="logoMargin" /> | `number` | — | The margin around the logo, in pixels. |
| <ApiLink pkg="core" type="QrCode" member="dotStyle" label="dotStyle" /> | `'square' \| 'circle' \| 'rounded'` | `'square'` | The shape of the data modules and the inner finder-pattern corner dot. |
| <ApiLink pkg="core" type="QrCode" member="squareStyle" label="squareStyle" /> | `'square' \| 'circle' \| 'rounded'` | `'square'` | The shape of the outer finder-pattern corner squares. |

</PlatformBlock>

<PlatformBlock for="Angular">

| Name | Type | Default | Description |
| -- | -- | -- | -- |
| <ApiLink type="QrCode" member="value" label="value" /> | `string` | — | The string encoded in the QR code. Clearing the value removes the SVG. |
| <ApiLink type="QrCode" member="version" label="version" /> | `number` | — | The QR version (1–40) used to generate the code. When not set, the smallest version that fits `value` is chosen automatically. |
| <ApiLink type="QrCode" member="errorLevel" label="errorLevel" /> | `'L' \| 'M' \| 'Q' \| 'H'` | `'M'` | The error correction level used when generating the QR code. |
| <ApiLink type="QrCode" member="size" label="size" /> | `number` | `128` | The rendered width and height of the QR code in pixels. |
| <ApiLink type="QrCode" member="margin" label="margin" /> | `number` | `4` | The quiet-zone margin around the code, expressed in QR modules. |
| <ApiLink type="QrCode" member="logoSrc" label="logoSrc" /> | `string` | — | The source URL of an optional logo image rendered at the center of the QR code. |
| <ApiLink type="QrCode" member="logoSize" label="logoSize" /> | `number` | `0.4` | The logo size as a ratio (0–1) of the safe area that can be obscured while remaining scannable. |
| <ApiLink type="QrCode" member="logoMargin" label="logoMargin" /> | `number` | — | The margin around the logo, in pixels. |
| <ApiLink type="QrCode" member="dotStyle" label="dotStyle" /> | `'square' \| 'circle' \| 'rounded'` | `'square'` | The shape of the data modules and the inner finder-pattern corner dot. |
| <ApiLink type="QrCode" member="squareStyle" label="squareStyle" /> | `'square' \| 'circle' \| 'rounded'` | `'square'` | The shape of the outer finder-pattern corner squares. |

</PlatformBlock>

## Styling

<PlatformBlock for="Angular">

<Sample src="/data-display/qr-code-styling" height={340} position="center" noBorder={false} spacing="md" alt="Angular QR Code styling example" />

</PlatformBlock>

<PlatformBlock for="React,WebComponents,Blazor">

<Sample src="/inputs/qr-code/styling" height={340} position="center" noBorder={false} spacing="md" alt="{Platform} QR Code styling example" />

</PlatformBlock>

The {Platform} QR Code appearance is controlled through CSS variables and CSS parts. Colors do not automatically invert with the global theme — set them explicitly for dark-themed codes.

### Styling Variables

| Variable | What it changes |
| -- | -- |
| `--ig-qr-code-background` | The background color of the QR code. Defaults to `white`. |
| `--ig-qr-code-dark-color` | The color of the data modules, and the corner square/dot colors unless overridden. Defaults to `black`. |
| `--ig-qr-code-corner-square-color` | The color of the outer finder-pattern corner squares. Falls back to `--ig-qr-code-dark-color`. |
| `--ig-qr-code-corner-dot-color` | The color of the inner finder-pattern corner dots. Falls back to `--ig-qr-code-dark-color`. |

### Style Parts

<PlatformBlock for="WebComponents, React, Blazor">

| Part | What it styles |
| -- | -- |
| `background` | The background rectangle of the QR code. |
| `dots` | The data modules (dots) of the QR code. |
| `corner-square` | The outer finder-pattern corner squares. |
| `corner-dot` | The inner finder-pattern corner dots. |

</PlatformBlock>

<PlatformBlock for="Angular">

| Selector | Description |
| -- | -- |
| `igx-qr-code` | The QR Code host element. |
| `.igx-qr-code__background` | The background rectangle of the QR code. |
| `.igx-qr-code__dots` | The data modules of the QR code. |
| `.igx-qr-code__corner-square` | The outer finder-pattern corner squares. |
| `.igx-qr-code__corner-dot` | The inner finder-pattern corner dots. |

</PlatformBlock>

### Sass Theming

Use the {ProductName} theme system to style the QR Code consistently with the rest of your application. Wrap the QR Code inside a section that scopes the CSS custom properties, or set them directly on the host.

<PlatformBlock for="WebComponents, React, Blazor">

```scss
@use "igniteui-theming/sass/themes" as *;

.branded {
  --ig-qr-code-background: var(--ig-surface-500);
  --ig-qr-code-dark-color: var(--ig-primary-500);
  --ig-qr-code-corner-square-color: var(--ig-primary-700);
}
```

</PlatformBlock>

<PlatformBlock for="Angular">

```scss
@use "igniteui-angular/theming" as *;

.branded {
  --ig-qr-code-background: var(--ig-surface-500);
  --ig-qr-code-dark-color: var(--ig-primary-500);
  --ig-qr-code-corner-square-color: var(--ig-primary-700);
}
```

</PlatformBlock>

### CSS Variables

Set component CSS variables directly when you need local styling without a Sass build step.

<PlatformBlock for="WebComponents, React, Blazor">

```css
igc-qr-code::part(background) {
  rx: 8;
  ry: 8;
}

igc-qr-code::part(dots) {
  filter: drop-shadow(0 1px 0 rgba(0, 0, 0, 0.15));
}
```

</PlatformBlock>

<PlatformBlock for="Angular">

```css
igx-qr-code {
  --ig-qr-code-background: #ffffff;
  --ig-qr-code-dark-color: #111827;
}
```

</PlatformBlock>

### Styling with Tailwind

Combine Tailwind utility classes with the QR Code CSS variables for utility-first styling. Make sure to [set up Tailwind](/themes/tailwind) first, then import the Ignite UI utilities in your global stylesheet:

```css
@import "tailwindcss";
@import "igniteui-theming/tailwind/utilities/material.css";
```

<PlatformBlock for="WebComponents">

```html
<igc-qr-code
  value="https://www.infragistics.com"
  class="![--ig-qr-code-background:theme(colors.slate.50)] ![--ig-qr-code-dark-color:theme(colors.slate.900)]">
</igc-qr-code>
```

</PlatformBlock>

<PlatformBlock for="React">

```jsx
<IgrQrCode
  value="https://www.infragistics.com"
  className="![--ig-qr-code-background:theme(colors.slate.50)] ![--ig-qr-code-dark-color:theme(colors.slate.900)]">
</IgrQrCode>
```

</PlatformBlock>

<PlatformBlock for="Blazor">

```razor
<IgbQrCode
  Value="https://www.infragistics.com"
  Class="![--ig-qr-code-background:theme(colors.slate.50)] ![--ig-qr-code-dark-color:theme(colors.slate.900)]">
</IgbQrCode>
```

</PlatformBlock>

<PlatformBlock for="Angular">

```html
<igx-qr-code
  value="https://www.infragistics.com"
  class="![--ig-qr-code-background:theme(colors.slate.50)] ![--ig-qr-code-dark-color:theme(colors.slate.900)]">
</igx-qr-code>
```

</PlatformBlock>

The exclamation mark (`!`) gives the Tailwind utility precedence over the component's default styles.

## Accessibility

The {Platform} QR Code is a static, non-interactive graphic that is announced by screen readers through an SVG `<title>` element.

### Keyboard Interaction

The QR Code does not receive focus and has no keyboard interaction.

| Key | Action |
| -- | -- |
| n/a | The QR Code is not keyboard interactive. |

### Screen Readers / ARIA

<PlatformBlock for="WebComponents, React, Blazor">

- The rendered `<svg>` element has `role="img"` and contains a `<title>` element describing the code.
- The default `<title>` content is `QR code: <value>`.
- Set `aria-label` to override the announced text with a more descriptive phrase such as "Scan to visit our product page".
- The QR Code does not participate in the tab order and does not respond to keyboard input.

</PlatformBlock>

<PlatformBlock for="Angular">

- The rendered `<svg>` element has `role="img"` and contains a `<title>` element describing the code.
- The default `<title>` content is `QR code: <value>`.
- Set the `ariaLabel` input to override the announced text with a more descriptive phrase.
- The QR Code does not participate in the tab order and does not respond to keyboard input.

</PlatformBlock>

### Accessibility Compliance

Infragistics documents {ProductName} accessibility support for Section 508 and WCAG 2.1 guideline areas in the [Accessibility Compliance](../interactivity/accessibility-compliance.mdx) topic.

| Criterion | How the component complies |
| -- | -- |
| [1.1.1 Non-text Content](https://www.w3.org/WAI/WCAG21/Understanding/non-text-content) | The rendered SVG carries a `<title>` element derived from `value` or an explicit `aria-label`, giving assistive technology a text alternative. |
| [1.4.3 Contrast](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum) | Default colors (black modules on a white background) meet the minimum contrast required for reliable scanning. |
| [4.1.2 Name, Role, Value](https://www.w3.org/WAI/WCAG21/Understanding/name-role-value) | The SVG exposes `role="img"` and an accessible name through its `<title>` element. |

Your responsibilities:

- Pair the QR Code with a short visible call to action so sighted users know what will happen after scanning.
- Provide an `aria-label` when the encoded `value` is opaque (e.g. a token or short identifier) so the announced text is meaningful.
- Keep sufficient contrast between the module color and the background when overriding CSS variables so scanners can read the code reliably.

## Troubleshooting

The {Platform} QR Code troubleshooting guidance follows a problem → cause → fix format for common integration and rendering issues.

### Why is my QR code not scanning?

Contrast between the modules and the background is too low, the code is too small on screen, or the quiet-zone margin is too small. Restore the default `black` on `white` colors, increase `size`, and use a `margin` of at least `4` modules for reliable scanning.

### Why does adding a logo make the code stop scanning?

The requested `logoSize` covers more of the code than the current `errorLevel` can compensate for. Lower `logoSize`, or leave `errorLevel` unset and let the component escalate automatically to the smallest level that fits the logo.

### Why is my logo not showing up?

`logoSrc` points to an unsafe scheme (such as `javascript:` or `vbscript:`), a non-image `data:` URI, or an image that failed to load. Replace the source with a valid image URL or a `data:image/*` URI; assigning a valid source afterwards recovers the rendered logo.

### Why did my QR code not switch to a dark theme automatically?

QR Code colors do not automatically invert with the global light/dark theme, because inverted codes are not universally scannable. Set `--ig-qr-code-background` and `--ig-qr-code-dark-color` explicitly for the dark variant of your UI.

### Why does the code disappear when I clear the value?

The component renders nothing when `value` is not set. Assigning any non-empty string to `value` re-renders the SVG.

## Known Limitations

The {Platform} QR Code has the following platform-independent limitations.

- The component does not automatically invert its colors when the global theme switches to a dark variant; light-on-dark QR codes must be configured explicitly through the CSS variables.
- The QR Code is a purely presentational graphic — it has no methods, events, or slots and does not participate in the tab order.
- QR Code visual structure follows a fixed, standardized layout and is not mirrored in Right-to-Left contexts.
- Unsafe `logoSrc` schemes (`javascript:`, `vbscript:`) and non-image `data:` URIs are rejected; no image is rendered in those cases.

## API References

Use these API references for the complete QR Code API surface.

<PlatformBlock for="WebComponents, React, Blazor">

<ApiLink pkg="core" type="QrCode" />

</PlatformBlock>

<PlatformBlock for="Angular">

<ApiLink type="QrCode" />

</PlatformBlock>

## Dependencies

The {Platform} QR Code requires a theme stylesheet to apply its default styling. See the framework-specific setup in **Getting Started**. The component generates QR data internally and has no external QR-generation dependencies or network requests.

## Additional Resources

Use these resources for support and related Ignite UI documentation.

- [{ProductName} **Forums**]({ForumsLink})
- [{ProductName} **GitHub**]({GithubLink})

## Related Components

The QR Code is a standalone display primitive. When you need related visual content, consider the following components:

- [Icon](../layouts/icon.mdx) — For static symbolic imagery that does not need to be scannable.
- [Avatar](../layouts/avatar.mdx) — For identity imagery representing people or entities.

## FAQ

<Faq>
  <FaqItem question="What data can I encode in a QR Code?" indicatorPosition="end">
    The `value` property accepts any string. Common uses include URLs, plain text, payment references, one-time setup tokens, Wi-Fi credentials, and vCard payloads. The component automatically selects the most compact encoding mode (numeric, alphanumeric, or byte) that fits the input.
  </FaqItem>

  <FaqItem question="Do I need an internet connection or an external library to render a QR Code?" indicatorPosition="end">
    No. The QR Code component generates the SVG entirely on the client from the string `value`, with no dependency on external QR-generation libraries or network requests. It works offline and inside sandboxed environments.
  </FaqItem>

  <FaqItem question="How large can the center logo be without breaking the code?" indicatorPosition="end">
    The `logoSize` ratio (default `0.4`) is applied to the safe area allowed by the current `errorLevel`. When `errorLevel` is not explicitly set, the component escalates to the smallest level that keeps the code scannable at the requested `logoSize`. Combine a high `errorLevel` (`Q` or `H`) with a smaller `logoSize` for the most reliable scanning.
  </FaqItem>

  <FaqItem question="Which package should I install for QR Code?" indicatorPosition="end">
    Use `igniteui-angular` for Angular, `igniteui-react` for React, `igniteui-webcomponents` for Web Components, and `IgniteUI.Blazor` for Blazor. Keep related Ignite UI packages on the same release version.
  </FaqItem>
</Faq>
