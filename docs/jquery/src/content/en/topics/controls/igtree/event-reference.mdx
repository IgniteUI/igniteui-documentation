---
title: "Event Reference (igTree)"
slug: igtree-event-reference
---
import ApiLink from 'docs-template/components/mdx/ApiLink.astro';

# Event Reference (igTree)

## Topic Overview

### Purpose

The topic that introduces the user to the `igTree`™ control’s events.

#### In this topic

This topic contains the following sections:

-   [**Event Reference**](#events)
-   [**Attaching Event Handlers in jQuery and MVC**](#attaching-handlers)
    -   [Attaching Event Handlers in jQuery](#attaching-handlers-jquery)
                -   [Attaching Event Handlers in MVC](#attaching-handlers-mvc)
-   [**Related Content**](#related-content)
                -   [Topics](#topics)

	
## Event Reference

The following table summarizes the purpose and functionality of the `igTree` control’s featured events:

Event | Description | Cancelable
------|-------------|-----------
<ApiLink type="igtree" member="dataBinding" section="events" label="dataBinding" /> | Event is raised before databinding is performed. | False
<ApiLink type="igtree" member="dataBound" section="events" label="dataBound" /> | Event is raised after databinding is finished. | False
<ApiLink type="igtree" member="drag" section="events" label="drag" /> | Event is raised on node drag. | True
<ApiLink type="igtree" member="dragStart" section="events" label="dragStart" /> | Event is raised on node drag start. | True
<ApiLink type="igtree" member="dragStop" section="events" label="dragStop" /> | Event is raised after a drag operation has completed. | False
<ApiLink type="igtree" member="nodeCheckstateChanged" section="events" label="nodeCheckstateChanged" /> | Event is raised after the checkstate of a node is changed. | False
<ApiLink type="igtree" member="nodeCheckstateChanging" section="events" label="nodeCheckstateChanging" /> | Event is raised before the checkbox state of a node is changed. | True
<ApiLink type="igtree" member="nodeClick" section="events" label="nodeClick" /> | Event is raised on node click. | False
<ApiLink type="igtree" member="nodeCollapsed" section="events" label="nodeCollapsed" /> | Event is raised after a node is collapsed. | False
<ApiLink type="igtree" member="nodeCollapsing" section="events" label="nodeCollapsing" /> | Event is raised before a node is collapsed. | True
<ApiLink type="igtree" member="nodeDoubleClick" section="events" label="nodeDoubleClick" /> | Event is raised on node double click. | False
<ApiLink type="igtree" member="nodeDropped" section="events" label="nodeDropped" /> | Event is raised after a node is dropped. | False
<ApiLink type="igtree" member="nodeDropping" section="events" label="nodeDropping" /> | Event is raised before a node is dropped. | True
<ApiLink type="igtree" member="nodeExpanded" section="events" label="nodeExpanded" /> | Event is raised after a node is expanded. | False
<ApiLink type="igtree" member="nodeExpanding" section="events" label="nodeExpanding" /> | Event is raised before a node is expanded. | True
<ApiLink type="igtree" member="nodePopulated" section="events" label="nodePopulated" /> | Event is raised after the children of a node are populated in the case of load on demand. | False
<ApiLink type="igtree" member="nodePopulating" section="events" label="nodePopulating" /> | Event is raised before the children of a node are populated in the case of load on demand. | True
<ApiLink type="igtree" member="rendered" section="events" label="rendered" /> | Event is raised after rendering of the tree has finished. | False
<ApiLink type="igtree" member="rendering" section="events" label="rendering" /> | Event is raised before rendering of the tree begins. | False
<ApiLink type="igtree" member="selectionChanged" section="events" label="selectionChanged" /> | Event is raised after a new node is selected. | False
<ApiLink type="igtree" member="selectionChanging" section="events" label="selectionChanging" /> | Event is raised before a new node is selected. | True


## Attaching Event Handlers in jQuery and MVC

### Attaching Event Handlers in jQuery

To attach to an event, just define a handler for it in the same way you define a property. Now if the event is triggered, the handler will be called.

**In JavaScript:**

```js
$("#igTree1").igTree({
      selectionChanging: function (e, args) {
           // Handle event  
      }
});
```

The following example shows how this is done and it also demonstrates the use of the jQuery's [`on`](http://api.jquery.com/on/) method to attach an event handler to a selected element:

<div class="embed-sample">
   [&#123;environment:SamplesEmbedUrl&#125;/tree-control/api-and-events](&#123;environment:SamplesEmbedUrl&#125;/tree-control/api-and-events)
</div>

### Attaching Event Handlers in MVC

When you attach a handler in MVC, use the jQuery User Interface (UI) pattern for attaching handlers to widget events. This means you should use jQuery’s [`on`](http://api.jquery.com/on/), [`bind`](http://api.jquery.com/bind/) or [`live`](http://api.jquery.com/live/) functions and pass the event’s name to them. The event name should be a lowercase string that is a concatenation of the name of the control and the event. You can do the same with the jQuery widget as well, but this would not be necessary because you can attach the event handlers directly when initializing the control. For more information, see the [jQuery widget factory](http://wiki.jqueryui.com/w/page/12138135/Widget%20factory). The code bellow demonstrates how to attach handlers to igTree events when implementing a MVC solution.

**In JavaScript:**

```js
$("#igTree1").on({ igtreedragstart: function (e, args) {
     // Handle event  
}});
```

## Related Content

### Topics

The following topics provide additional information related to this topic:

-	[igTree Overview](/igtree-overview.mdx)
-   <ApiLink type="igtree" label="igTree jQuery API documentation" />
-   [igTree ASP.NET MVC API documentation](Infragistics.Web.Mvc~Infragistics.Web.Mvc.TreeModel_members.html)
-	[Using Events in &#123;environment:ProductName&#125;](//general-and-getting-started/using-events-in-igniteui-for-jquery.mdx)